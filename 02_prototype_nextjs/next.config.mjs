/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  turbopack: {
    // Turbopack is stable in v16
  },
  experimental: {
    // Add any experimental flags if needed, but keeping it clean for now
  }
};

export default nextConfig;
